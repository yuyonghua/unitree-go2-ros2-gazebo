# Handoff — GO2 L1 复现（Gazebo 建图+导航，手写复刻）

Date: 2026-09-04. Lang: Chinese. Repos: original
`/home/user/git/ros2-learning/ROS2-Gazebo-GO2` (reference only, runs OK),
repro `~/git/yyh/go2_ws` (active work). ROS 2 Humble, `rmw_cyclonedds_cpp`.

## Goal & standing decisions (do not relitigate without asking)

- Rebuild from scratch in `go2_l1_ws`-style ws (`~/git/yyh/go2_ws`): copy only
  meshes/leg xacro/controllers/worlds; hand-write the rest.
- L1 (`/robot1/scan`, front ~200°) is the SLAM/Nav2 sensor; external 360°
  lidar is optional behind `use_external_lidar` (default false).
- `quadropted_controller` + `quadropted_msgs`: reuse as-is, never rewrite.
- SLAM: `slam_toolbox` (not cartographer). Package responsibilities:
  `go2_description` = static model only (never touches Gazebo);
  `go2_gazebo_bringup` = everything simulation.
- xacro files align with official Unitree ROS1 names
  (`/home/user/unitree-ws/sdk/unitree_ros/robots/go2_description`):
  `robot.xacro` + `gazebo.xacro` (+ `lidar_external.xacro` for the optional lidar).

## Canonical artifacts (read these, don't duplicate)

- Full guide: `/home/user/git/ros2-learning/ROS2-Gazebo-GO2/GO2-L1-复现指南.md`
  (§4 description ✅ done; §5 bringup P2 — user is testing NOW; §7–8 slam/nav2 not started).
- Repro ws layout: `~/git/yyh/go2_ws/src/{go2_description,go2_gazebo_bringup(worlds only + launch),go2_nav,go2_slam,quadropted_controller,quadropted_msgs}`.
- Upstream analysis (dual-lidar wiring, topic/TF map): guide §1 — original
  mapping/SLAM/Nav2 all use `/velodyne`; switching to L1 means changing
  `scan_topic` in slam launch + 3 spots in `go2_nav2.yaml`.

## Last session state (what's pending)

- User is about to test (their run, not ours):
  `colcon build --packages-select go2_description go2_gazebo_bringup`
  then `ros2 launch go2_gazebo_bringup launch.py use_external_lidar:=true`.
- Just fixed (unverified by execution — user tests): empty `<ros2_control>`
  block in `gazebo.xacro` filled with all 12 joints + friction blocks;
  `l1.launch.py`/`launch.py` rewritten in commented readable style.
- Known past failures, all diagnosed: spawn `-topic` must match rsp namespace
  (`/robot1/robot_description` when namespaced); RViz `Fixed Frame=base_link`
  (stale `~/.rviz2/default.rviz` once pointed at `velodyne` — user-side config,
  not a repo bug); `use_external_lidar` needs OpaqueFunction+perform for xacro
  mappings; `install/` keeps stale files after removals (delete manually).
- User constraints that still apply: work piece-by-piece (don't jump ahead to
  slam/nav2); don't run long sim tests unprompted (background `pkill -f`
  patterns can match your own shell — use bracket trick or exact PIDs);
  keep code heavily commented in Chinese; no rviz config files in repo;
  keep guide doc in sync with real files.

## Suggested skills (call via Skill tool)

- `tdd` — when adding slam/nav2 wiring or touching odom/EKF chain; verify
  each stage (`/robot1/scan` 10Hz → `odometry/filtered` 30Hz → `/map` → Nav2 goal).
- `grill-me` — before writing P2 bringup final or Nav2 params, to stress-test
  the L1 narrow-FOV assumptions (blind rear, 10m range cap, inflation 0.55).
- `prototype` — if user wants to preview a launch/TF topology change before
  committing to it.
- `hermes-notes-export` — if user asks to export this session history.
- `find-skills` — if user asks for capabilities outside current skills.

## Watch-outs

- `leg.xacro` has upstream typo `<joint ... fixed">0` (tolerated by parser) —
  leave it, matches original.
- `meshes/` (10 files): only trunk/hip/thigh/thigh_mirror/calf are referenced;
  `Vlp16.dae` needed only when external lidar on; `dae/` dir is a duplicate —
  never create it. `calf_mirror/foot.dae` are dead files.
- Guide doc §4.3 previously said "copy omitted lines" and user copied the
  comment literally — doc now contains full content; keep full content in docs.
- No secrets encountered; nothing to redact.
